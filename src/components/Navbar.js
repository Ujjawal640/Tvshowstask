import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <nav class="navbar bg-black">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 h1 text-white px-3 fw-bold ">TVShows</span>
  
            
  </div>
</nav>
    
  );
};

export default Navbar;
